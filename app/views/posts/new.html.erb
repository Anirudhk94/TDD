<h1>New Post</h1>

<%= form_for @post do |f| %>
	<p>
		<%= f.label :title %>
		<%= f.text_field :title %>
	</p>

	<p>
		<%= f.label :caption %>
		<%= f.text_area :caption %>
	</p>

	<%= f.submit %>
<% end %>